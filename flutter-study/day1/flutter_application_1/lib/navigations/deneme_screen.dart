import 'package:flutter/material.dart';

class DenemeScreen extends StatelessWidget {
  const DenemeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text("denemedesiniz"),
    );
  }
}